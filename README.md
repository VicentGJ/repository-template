# Repository Template

Monorepo template featuring:

- NestJS backend in `apps/api`
- Next.js App Router web in `apps/web`
- Shared eslint/prettier/tsconfig via `packages/config`
- Turbo + pnpm workspaces
